# guide — методичка курсу: тексти сторінок сайту

Те, що студент читає на сайті: домашня, «Ігри», «Лаби» (+ 5 лаб), «Таски», «Бали», «Здача», «Що змінилось». Один файл = один розділ = одна сторінка або блок на сторінці. Чернетки з 2026-09-20 за пропозицією [T115](../../.dev/.t/T115-P--guide-on-site.md); питання — [T116](../../.dev/.t/T116-Q--guide-questions.md); походження — Notion-методичка (`data/v2/notion/python/`).

Редагують обидва (Vitalik + агент), NO-EDIT тут не діє; історія — git. Це джерело сайту: `GET /api/guide/{slug}` віддає файл як `{title, brief, body, updated}` (`updated` — дата зміни файла), сторінки рендерять його на льоту ([T117](../../.dev/.t/T117--guide-v1/report.md)). Правка = правка файла, збирати фронт не треба.

## Конвенції

- Markdown, UA. Перший `#` — назва сторінки; `##` — підрозділи (потрапляють у зміст); `###` — пункти.
- **Якір** `{#id}` у кінці заголовка або пункту списку — стабільне EN-імʼя для посилань (`/score#79`). Заголовок без `{#id}` отримає авто-id від тексту, але він зміниться разом із текстом — важливе якорити явно.
- **Стиль** — як у Notion-методичці Vitalik: короткі фрази, вкладені пункти замість довгих речень, жирним — ключове слово, емодзі на початку важливих пунктів, без багатослівʼя. Розмір тексту на всіх сторінках однаковий (без `lead`).
- `<!-- more -->` — необовʼязковий: усе вище нього API віддає ще й як `brief` у списку сторінок; сайт зараз показує сторінку цілком, тож маркер у файлах не потрібен.
- **Виноска** — цитата, що починається з емодзі (`> 💡 …`, `> ⚠️ …`) — рендериться як callout.
- **Монетки** — іконки сайту: 🌱 деревʼяна · 📎 оловʼяна · 🥉 бронзова · 🥈 срібна · 🥇 золота · 👑 корона (кожна = 2× попередньої).
- **Посилання** всередині сайту — відносні (`/tasks`, `/my/game`, `/score#defense`); текст лінка на розділ — як пункт меню (Ігри · Лаби · Таски · Бали · Здача); на сторінці «уся методичка» (`/method`) якорі отримують префікс розділу (`#score-defense`) — лінки `#id` у тексті переписуються самі.
- `🚧` — незакрите місце; список — у [T116](../../.dev/.t/T116-Q--guide-questions.md).

## Файли

| Файл | Де на сайті | Стан |
|---|---|---|
| [home.md](home.md) | `/` — лід + «Як влаштований курс» (посилання на розділи) + «З чого почати» | 💭 |
| [game.md](game.md) | `/games` — розділ «Ігри», уся сторінка (каталог поки лише адміну, T116 Q13) | 💭 |
| [labs.md](labs.md) | `/labs` — огляд лаб | 💭 |
| [lab1.md](lab1.md) … [lab5.md](lab5.md) | `/labs/1` … `/labs/5` | lab1 🌿 (з Notion) · lab2–5 💭 (чернетка від тем лекцій) |
| [tasks.md](tasks.md) | `/tasks` — уся сторінка (каталог поки лише адміну) | 💭 |
| [score.md](score.md) | `/score` | 💭 (пороги — з Notion, монетки на пороги — 🚧; slug змінюється разом із рядком у `guide.js`) |
| [howto.md](howto.md) | `/howto` (+ словник) | 💭 |
| [changes.md](changes.md) | `/changes` — «Що змінилось», лінк з домашньої (не в меню) | 💭 |
