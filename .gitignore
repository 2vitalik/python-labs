# Витяги сесій — похідний артефакт (регенерується extract.py), синк через Dropbox
.dev/.sessions/
# Драфти промптів та ideas-inbox — сирий голос, не для історії (T95)
.dev/.drafts/
__pycache__/
.idea/
.env
.venv/
node_modules/
app/vue/dist/
# Скриншоти студентських робіт — живуть поза git (синк Dropbox); .trash — кошик видалених
app/api/uploads/
app/api/uploads.trash/

*.hid.*
