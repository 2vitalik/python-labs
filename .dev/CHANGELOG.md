# CHANGELOG

- 2026-09-25 — **слід студента в Telegram: членство, реакції, `raw`, дата привʼязки** (запит Vitalik: «збирати, коли студент приєднується до форуму; реакції; все, що можна, окрім великих файлів; дату привʼязки; будь-яку активність»; план і звіт — [T138](tgbot/.t/T138--tg-footprint/)): `bot/log.py` — `chat_member`/`my_chat_member`/`chat_join_request` → `kind=member`, `message_reaction` → `kind=reaction`, `raw` для кожної вхідної події; `User.tg_linked_at`. Відповідь на «я вже просив раніше?» — так, сайт із T125 пише входи й кожен перегляд сторінки в `activity`, зміни — в `history`. Смоуки 15/15, 12/12, 20/20. Що ще збирати (API-виклики, user-agent, файли, GitHub, heartbeat…) — [T139](tgbot/.t/T139-Q--activity-more.md).
- 2026-09-25 — **журнал перевірено наживо** (питання Vitalik: «чи дійсно бот все зберігає — форум, повідомлення студентів боту, мої в приватному спілкуванні; обробляти постфактум — так?»): бот не був запущений, `messages` порожня → запущено, з черги Telegram прийшли службові події форуму, далі наживо текст у форумі, приват із ботом в обидва боки, повідомлення викладача через Chat Automation — усе в базі. Прогалина: повідомлення студента в бізнес-чаті не відрізнялось від повідомлення боту → `chat_type=business` для Chat Automation (`bot/log.py`, і для `deleted`), `smoke_log.py` **10/10**; тестовий рядок у базі перефарбувати руками. Обмеження: зберігається лише поки бот працює (Bot API історію не читає, черга ~доба), реакції й видалення в групах — ні; клієнт шле `/start bizChat<id>` з бізнес-чату — поки «не впізнаю». Постійний процес і bizChat — [T137](tgbot/.t/T137-Q--bot-always-on.md). Постфактум аналіз — так: сирі поля, `from_id` є й для непривʼязаних. З клік-тесту нотаток: `HINT` з `<текст>` ламав HTML (три 💥) → `html.quote`; `/hide` у чаті зі студентом не видалився («message can't be deleted», право «Delete all messages» не ввімкнене) → алерт ⚠️ показує 🔑 права і ☝️ що ввімкнути; `smoke_notes.py` **20/20**.
- 2026-09-25 — **методичка закрита від студентів до завершення** (Vitalik у чаті: «всі сторінки методички заховаємо… незавершений варіант не хочу показувати; але сайт показати вже треба, щоб вони зареєструвались на ньому та в телеграм-боті»; звіт — [T136](.t/T136--guide-hidden/report.md)): `GET /api/guide*` — лише адміну; маршрути `/labs` `/score` `/howto` `/changes` `/method` `/games` `/tasks` — `hidden = { access: 'admin' }` у `router.js`; домашня гостю/студенту — `StartSteps.vue` (вхід → бот, ✅/⬜; крок «профіль» прибрано за словом Vitalik після коміту, натомість примітка «GitHub поки можна не заповнювати — репозиторій створиш, коли обереш гру й назву проєкту»), меню студента — лише «Профіль». Смоук 21/21, build чистий. Перед показом — імпорт груп ЦІСТ, інакше студенти pending без профілю й бота; відкрити назад — одна правка в `router.js` + `current_user` у двох GET.
- 2026-09-24 — **підтвердження нотатки — реакція ✍** (питання Vitalik: «реакцію на моє /note або відредагувати моє повідомлення, додавши saved»): у форумі й у чаті з ботом — `setMessageReaction` ✍ на команду (fallback — ефемерне «✔️ Записав», коли реакції в групі вимкнені або команда ефемерна); у чаті зі студентом через Chat Automation Bot API 10.3 не дає ні реакції (`setMessageReaction` без `business_connection_id`), ні редагування чужого повідомлення — там лишається алерт. `smoke_notes.py` **19/19**. Коміт T135 — `83e23c1`.
- 2026-09-24 — **нотатки — лише для викладача** (Vitalik у чаті: «суто інформація для мене, щоб не забулось; писати студенту й згадувати не треба; для /note просто залишити як є»; звіт — [T135](tgbot/.t/T135--notes-simplify/report.md)): доставку T134 прибрано (🤖/📝, DM, відповідь у гілці, guest-нотатки, `note_show.py`, `Note.shown`); `/note` лишається в чаті, `/hide` зникає (Chat Automation) або ефемерний (форум); `Note.hidden`; права в Chat Automation — лише «Delete all messages». Факти: Guest Mode увімкнено, Chat Automation підключено через Desktop → Business → Chatbots, один бот на акаунт. Смоук `smoke_notes.py` **17/17**. Коміт T134 — `0c69fc0`.
- 2026-09-24 — **нотатки v2: `/note` видима, `/hide` тиха** (відповіді Vitalik на [T133](tgbot/.t/T133-Q--notes-choices.md) у чаті: замість «📝» дві команди, Q2 a, Q3 a; звіт — [T134](tgbot/.t/T134--notes-commands/report.md)): `/note` показує студенту найкращим шляхом — від імені викладача з 🤖 (Telegram ботові повідомлення від імені акаунта не позначає, перевірено), особисто від бота, відповіддю у гілці форуму, або від самого бота після guest-згадки `@python_nure_bot /note …`; `Note.shown`; доставка — `bot/note_show.py`; обидві команди ефемерні для адмінів груп. Зʼясовано: Guest Mode вмикається лише в міні-апці BotFather. Смоук `smoke_notes.py` **21/21**. Коміт T132–133 — `49dc65c`.
- 2026-09-24 — **Chat Automation, нотатки `/note`, ефемерні команди, Guest Mode** (відповіді Vitalik на [T131](tgbot/.t/T131-Q--message-log-2.md) у чаті: бот адмін форуму + privacy off, Chat Automation і зберігати все, лише вибрані студенти, дві команди — видима й ні, Guest про запас, ефемерні зараз; звіт — [T132](tgbot/.t/T132--chat-automation-notes/report.md)): `log.incoming` на `business_message`/`guest_message`, правки й видалення бізнес-чатів у `messages`; колекція `notes` (`Note`); `bot/notes.py` — `/note` у чаті зі студентом (тихо: `deleteBusinessMessages` + алерт), «📝 …» (видима), `/note` у форумі як ефемерна команда (`setMyCommands(is_ephemeral=True)` для адмінів груп, ефемерна відповідь) і в чаті з ботом; алерт про підключення з правами; вид `note`; guest-згадка → інтро. Смоук `smoke_notes.py` **16/16**. Решта виборів — [T133](tgbot/.t/T133-Q--notes-choices.md). Коміт попереднього кроку — `dd48f63`.
- 2026-09-24 — **журнал повідомлень бота в коді** (відповіді Vitalik на [T129](tgbot/.t/T129-Q--bot-message-log.md): Q1–Q2 «a», Q4 «поки a, потім обовʼязково»; Q3 переосмислено — йшлося про форум потоку): колекція `messages` (`TgMessage`), `bot/log.py` — outer-middleware на `dp.message` + хендлер `edited_message`, `notify.outgoing()` — session-middleware спільного `Bot` (вихідні з бота і з API, вид алерту через `ContextVar`); смоук `smoke_log.py` **9/9**; форум — після адмін-прав боту. **Дослідження «сигнал боту з приватного чату»** — [T130](tgbot/.t/T130-C--signal-from-private-chat.md): «нова фішка» = оновлення Telegram 2026-05-07 (Bot API 10.0) — Chat Automation (бот у власних приватних чатах, без Premium; читає обидві сторони, команду можна прибрати з чату) і Guest Bots (@згадка в будь-якому чаті, одна видима відповідь), з липня — ефемерні команди в групах (Bot API 10.2); aiogram 3.31 усе це вже вміє; рекомендація — Chat Automation для приватних чатів, ефемерні для форуму, Guest «про запас»; вибори — [T131](tgbot/.t/T131-Q--message-log-2.md). Побіжно: `smoke_notify` 23/25 і `smoke_events` 10/18 падають і на HEAD (тексти алертів після T125/T128) — окремо.
- 2026-09-24 — ідея Vitalik: бот зберігає історію повідомлень для аналізу (активність студентів, хто що питав). Оцінка — окремого проєктування не треба, ~50 рядків у трьох файлах: колекція `messages` за зразком `activity`, вхідні — outer-middleware диспетчера (`bot/log.py`), вихідні — session-middleware спільного `Bot` у `notify.py` (ловить і відповіді, і алерти з процесу API); вибори — обсяг, медіа через `file_id`, алерт `msg` про текст студента, де дивитись — [T129](tgbot/.t/T129-Q--bot-message-log.md).
- 2026-09-22 — **знахідки: `/ideas`, поля рядками, textarea, що росте; редактор — «редагувати», каретка на початку** (Vitalik у чаті: «Знахідки давай url буде /ideas… розібʼємо кожне текстове поле окремим рядком… текстове поле автоматично збільшується… кнопочка для редагування нехай буде не "сторінку"… текстове поле проскролене в кінець»): маршрут `/ideas` (`/refs` → редирект, лінки в каталогах і `tasks.md`); `GrowArea.vue` — textarea, що росте/зменшується за текстом (у формі знахідки, ідеї під картонкою, редагуванні картки), ⌘/Ctrl+Enter — надіслати, нотатки показуються з переносами; `GuideBody` — «✏️ редагувати» замість «сторінку»; `GuideEditor` — `setSelectionRange(0, 0)` перед фокусом (Chrome ставив каретку і скрол у кінець).

- 2026-09-22 — **редактор методички v1, історія з diff, чернетка «Що змінилось», поля-клітинки, ідеї в знахідках, `/my/profile`** (Vitalik у чаті: «Давай зробимо редактор, і всі інші штуки, все за твоїми рекомендаціями. І давай може поміняємо лінк /profile на /my/profile?»; план/звіт — [T128](platform/.t/T128--guide-editor-v1/plan.md) за [T126](platform/.t/T126-P--guide-editor-v1.md), вибори [T127](platform/.t/T127-Q--editor-refs-choices.md) — усі «а»): тексти методички переїхали в Mongo (`models/guide.py`, `guide_io.py` seed/export/import, `data/guide/*.md` — знімок); `PUT /api/guide/{slug}` з `rev` → 409 на застарілий, `history.note`; `GuideBody.vue` + `GuideEditor.vue` (Teleport у слот після заголовка, textarea + превʼю, нотатка, ⌘/Ctrl+Enter, guard незбереженого) на всіх сторінках з текстом методички; `/method/history` (`HistoryPage.vue`, `diff.js`, `Diff.vue`, «↩» відкат); чернетка `changes-draft` + `POST /api/guide/changes/publish` + `ChangesDraft.vue` на `/changes`; ` ```field ` → `field.js` сітка клітинок, 5 прикладів з `data/v2/notion/games/` у `game.md`; `refs.kind/parent`, `RefCard.vue`, фільтр, ідея під знахідкою; `/my/profile` + редирект, бот/тексти/тести; смоуки `smoke_guide.py` 19/19, bot 12/12, games 70/70; клік-тест у Chrome (розширення цього разу підключилось).

- 2026-09-21 — **зміст на /method, активність студентів, підсвітка пошуку, «Ігри»: 24 пункти після клік-тесту T124** (Vitalik у чаті: «на Уся методичка також треба Зміст справа… не лише seen_at, а й first_login_at… зберігати історію взаємодії студента із сайтом… першу версію редактора… не завжди клік по категорії повертає наверх… виділяти жовтим при пошуку…») → [T125](platform/.t/T125--ui-polish-4/report.md): `Toc.vue` на `/method`; «↑» на самий верх; `first_seen_at` + `last_seen_at` замість `seen_at` (міграція в Mongo); **колекція `activity`** — `login` при вході + `view` на кожен маршрут (`router.afterEach` → `POST /api/me/view`), правки вже в `history`; розділ «Гра» → **«👾 Ігри»** (`game.md`, `home.md`); «Що змінилось» — вступ; студи — заголовки по центру, «не заходив» справді сірий (`--bs-table-color`); крихти з «Методичка» на сторінках гри/редагування; «Завдання гри» сховано (`SHOW_TASKS`); списки методички лівіше (Notion); знахідки — «відео, стаття, сайт, сама гра»; шапка «Завдання» у два рядки (кнопки біля заголовка, пошук ширший); **підсвітка пошуку** `<mark>` у назві й описі з автовідкриттям картки, коли збіг не в назві; **фікс скролу по зоні** (скрол після перерендеру + `min-height` каталогу — коротка категорія «підтягувала» шапку вниз). Редактор методички (п. 32–35, 41, 45) — пропозиція [T126](platform/.t/T126-P--guide-editor-v1.md): варіант 3 з T115 (Mongo + інлайн-редактор `##`/`###`, `record()` + нотатка, `/method/history` з рядковим diff, чернетка «Що змінилось», ` ```field `-блоки → `Field.vue` клітинками, бо емодзі в `<pre>` зʼїжджають між ОС); вибори + ідеї в знахідках (п. 49) — [T127](platform/.t/T127-Q--editor-refs-choices.md).

- 2026-09-21 — **/method, зміст справа, широкі Таски, студи: 27 правок після клік-тесту T122** (Vitalik у чаті: «для усієї методички посилання /method… у крихти додамо методичку… зміст у методичці ще трішки-трішки… кнопочку наверх… Таски вузькою як інші, і зправа Зміст… кнопочка розгорнути на всю сторінку… в меню завжди іконка додому… студи: нумерація рядків, без Статусу, клік лише по імені…») → [T124](platform/.t/T124--ui-polish-3/report.md): `/all` → **`/method`** (`MethodPage.vue`; зміст щільніший, «↗» зліва від рядка і від заголовка розділу з підказкою «Відкрити розділ окремою сторінкою», «↑ Наверх, до змісту» поруч із 🔗); крихти розділів — `🏠 › Методичка › …` (`GuidePage`, `/games`, `/tasks`), «📖 Уся методичка» внизу лишається; **`Toc.vue`** — один фіксований «Зміст» справа для `GuidePage`, `/games`, `/tasks` (заголовки тексту блоку, поки він відкритий, + зони каталогу; `GuideHead` віддає свій зміст подією); **`/tasks` у колонці сторінки** + кнопка «на всю ширину» ↔ (`wide.js`, `localStorage tasks-wide`; крихти й блок тексту лишаються в колонці, широкий — лише каталог; зміст справа в широкому режимі ховається); `Crumbs` зі слотом праворуч — «Моя гра» на `/games`+`/tasks`; бренд меню — завжди 🏠 (`stage 2` тепер лише про «Вийти»); шеврони-SVG (`IconChevron`) замість ▴▾/▸▾, «згорнути» синім; callout — рівні відступи; «Що змінилось» на домашній знову сіре (`:slotted`); `home.md` — Лаби перед Грою; Lode Runner 🪙 → 🪜 (YAML + Mongo); `guideClick` зберігає `?query` при `#hash` (раніше якір скидав фільтри каталогу); **студи** — щільні рядки, пошта світліша, нумерація в групі CSS-лічильником поза таблицею, імʼя = лінк на профіль замість кліку по рядку (hover прибрано), колонки «Статус» нема — pending/admin фарбують рядок (+бейдж), «не заходив» — сірий рядок (`seen` в API), Telegram показує лише брак: `@нік 🚫` / *без нікнейма* / 🚫 / «—» по центру. `npm run build` чистий; DOM-перевірка headless Chrome гостем і адміном.

- 2026-09-21 — **уся методичка, студи, каталог, бот: 20 правок після клік-тесту T121** (Vitalik у чаті: «↗ у змісті відокремити і в бордюрчик… зміст меншим шрифтом… іконка „Що змінилось" досі не та… „Привіт, Віталій!"… крихти вище… інший URL замість /guide… заголовки на /guide неоднозначні — іконками-кнопками… „Усі лаби" не по центру… „Уся методичка" під кожним розділом… текст на Іграх/Тасках видимий, у рамці, з памʼяттю… Лаби й Ігри місцями… підпис до плюсика… клік по зоні — не нагору… аватар і імʼя — у профіль… Студи: таблиця за замовчуванням, ПІБ+пошта, групування зі згортанням, група на підсторінці, телеграм клікабельний… callout жовтенький… спалах якоря не завжди… перепитувати перед відвʼязкою»; звіт — [T122](platform/.t/T122--guide-students-polish/report.md), вибори — [T123](platform/.t/T123-Q--polish-choices.md)): `/guide` → **`/all`**; `.ibtn` — глобальна рамочна іконка-кнопка (зміст `/all` у рамці fit-content дрібним шрифтом, «🔗»/«↗» праворуч заголовків розділів, «↗» групи); `GuideFoot.vue` — «Оновлено» + «📖 Уся методичка» під усіма guide-сторінками, домашньою і текстом на `/games`/`/tasks`; `GuideHead` — `<details open>` у рамці з «згорнути ▴/розгорнути ▾» і памʼяттю в `localStorage`; `.labs-nav` grid `1fr auto 1fr`; `guide.js` — Лаби · Ігри · Таски · Бали · Здача; `scrollBehavior` — `?query` на `meta.filters`-сторінках не скролить, зона/підзона → `#catalog`; «＋ таск»; аватар+імʼя → `/profile`; `StudentsTable.vue` — `<tbody>` на групу зі згортанням, «згорнути/розгорнути всі», `?group=` з крихтою, ПІБ+пошта в одній клітинці, `t.me`-лінки (і «відкрити ↗» у формі); callout на `warning`, блідіший за спалах; спалах цілі — `GuideText` сам + один watch хешу в `App.vue` (домашня не мала свого — тому не спалахувала), `.flash` глобальний (розділ на `/all` раніше клас отримував без стилю); «Відвʼязати бота? [Так] ні»; «Привіт, …!», крихти `-.6rem/1rem`, ✨. Headless-Chrome DOM-перевірка гостем і адміном (dev-юзер тимчасово admin, повернуто), build чистий.

- 2026-09-20 — **крихти замість «Назад», активний пункт меню, дрібне шліфування** (Vitalik у чаті після клік-тесту T118: «активний пункт меню якось краще виділявся… шлях „Іконка домой → Лаби → Лаба 1“ усюди, тоді „назад“ не потрібен… „Оновлено“ вправо і світло-сіре… „Привіт, Віталій“… іконка для Здача інша… games/slug стандартною шириною»; звіт — [T121](platform/.t/T121--nav-crumbs-polish/report.md)): `Crumbs.vue` + `IconHome.vue` — `🏠 › батьки › сторінка` на всіх сторінках, «← Назад/До ігор/До каталогу/До списку/До студентів» прибрано; `NavBar` — `active` за шляхом і синя риска під словом (`::before`, бо `::after` — стрілка «Ще»), у дропдауні теж; «Оновлено» праворуч `text-body-tertiary`; домашня — «Привіт, {first_name}» (для `pending` — підказка про доступ), «Що змінилось» (назва з `changes.md`) і «📖 Уся методичка» двома рядками праворуч; `/guide` — рамка-зміст з якорями вниз і «↗» окремо; іконки «Здача» 📤→✅, «Що змінилось» 🆕→📰; `/games/<slug>` у 52rem, `/tasks` широкий лише адміну, залишки `col-lg-*` з 8 сторінок прибрано. Відступи меню `.3rem` на телефоні виміряно — складу рядка не змінюють, не залишено. Chrome-клік-тест 1320 + 390-фрейм, студент/адмін/гість; build чистий. T119 Q3 фактично вирішено запитом.

- 2026-09-20 — **меню коротше з «Ще», єдина ширина сторінки, тексти в стилі Notion** (Vitalik у чаті після клік-тесту: «перейменуємо меню коротше… останні пункти згортати в менюшку „Ще" замість гамбургера… ширину сторінки як у профілю… текст ближче до мого стилю з Notion»; звіт — [T118](platform/.t/T118--nav-width-polish/report.md), питання — [T119](platform/.t/T119-Q--nav-naming-questions.md)): меню Ігри · Лаби · Таски · Бали · Здача · Студи · Моя гра · Профіль в один рядок завжди — `navFit.js` міряє пункти і по черзі ховає імʼя, стискає бренд/«Вийти» до іконок, хвіст пунктів кладе в дропдаун «Ще»; «Знахідки» — кнопкою на адмінських `/games` і `/tasks`; `Avatar.vue` з `no-referrer` і кружечком-ініціалом на збої (розбір — [T120](platform/.t/T120-C--avatar-glitch.md), не відтворилось); `--page-max: 52rem` + `.page` для всіх сторінок і навбара замість `container`/`col-lg-*`; домашня — `home.md` цілком без розкривашок і `lead` (`GuideBrief` видалено), «Що змінилось» — `/changes`; зміст guide-сторінки — фіксований у правому полі по центру, сірий, «Зміст», від 1216 px; профіль — червоні мітки без даних, «Telegram». Тексти `data/guide/` (12 файлів) переписано коротко, вкладеними пунктами; заголовки = пункти меню. Chrome-клік-тест на 1320/864/700/480/390, build чистий.

- 2026-09-20 — **методичка на сайті v1 — у коді** (Vitalik у чаті: «максимальний варіант, усі зауваження, повний план окрім деплоя і редактора; тексти правитиму сам потім»; звіт — [T117](.t/T117--guide-v1/report.md), відповіді — [T116](.t/T116-Q--guide-questions.md)): `routes/guide.py` — `GET /api/guide` + `/api/guide/{slug}` з `data/guide/*.md` (`title/brief/body/updated`, кеш по mtime); фронт — `guide.js` (список розділів = маршрути = меню), `md.js` (свій `Marked`: `{#id}` → id заголовка + «🔗», якір у пункті — `<span>`, авто-id транслітерацією, `> 💡` → callout, таблиці в `table-responsive`, зовнішні лінки в нову вкладку), `anchors.js` (скрол до цілі після завантаження тексту, відкриття `<details>`, спалах секції 2.5 с, reduced-motion, копіювання лінка, внутрішні лінки через router); сторінки `GuidePage` (зміст праворуч на `lg+`, «оновлено», ← → між лабами), `GuideAllPage` `/guide` (усе однією сторінкою, id з префіксом розділу), домашня = лід + `<details>`-витяги + «З чого почати» + «Що змінилось» (`changes.md`; місце вставки — маркер `<!-- sections -->` у `home.md`); `/games` і `/tasks` публічні — текст розділу всім, каталог лише адміну, іншим заглушка (Q13); `/grading` → `/score` (Q2, варіанти в звіті); навбар — тоглер на Vue для телефонів; `scrollBehavior` — нагору при переході. Тексти: `home.md` під нову домашню, Q12 (групові захисти, чат за заявкою) у `howto.md`/`score.md`, словник. Перевірка: build чистий, 20 перевірок рендера, headless-Chrome DOM 8 сторінок; клік-тест у Chrome не вдався (розширення не підключене) — розкривашки/копіювання/тоглер перевірені лише кодом і DOM.

- 2026-09-20 — **методичка на сайті: пропозиція і чернетки** (Vitalik у чаті: «перенести методичку з Notion красиво на сайт… на домашній коротко, розкривашки, окремі сторінки, посилання на підрозділ із підсвіткою, публічно, пари скоро»): [T115](.t/T115-P--guide-on-site.md) — сайт замість Notion (публічно), сторінки `/labs`+`/labs/1…5`, `/grading`, `/howto`, витяги «Гра»/«Завдання» в шапках каталогів, домашня = лід + `<details>`-витяги + «з чого почати»; зберігання — `data/guide/*.md` через `GET /api/guide/{slug}` (потім Mongo + редактор за зразком каталогу, 5 варіантів редагування зважено), якорі `{#id}` → `/grading#79`, «🔗» копіює лінк, спалах секції-цілі через `scrollBehavior`; мапа Notion → сайт (Coda прибрано, «−1» = стеля, 6 монет сайту), 12 «не врахував» (деплой як передумова, лаби 2–5 не написані, версія семестру, стабільність якорів, особистий екран). Чернетки всіх текстів — `data/guide/` (12 файлів, README з конвенціями: `<!-- more -->` витяг/повне, `> 💡` виноски; lab1 — з Notion, lab2–5 — від тем лекцій з 🚧). Питання — [T116](.t/T116-Q--guide-questions.md) (15, з рекомендаціями за замовчуванням). Джерело: `data/v2/notion/python/`.

- 2026-09-20 — **сторінка входу з поверненням на цільову** (Vitalik у чаті: «посилання на недоступні сторінки кидають на домашню — не добре; окрема сторінка з повідомленням і кнопкою, після входу — на ту сторінку»; звіт — [T114](platform/.t/T114--login-next/report.md)): стандартний `?next=` — guard → `/login?next=<шлях>`, кнопка → `/api/auth/login?next=`, бекенд несе `next` через сесію повз Google і редиректить туди; `LoginPage.vue` на три стани (гість · `pending` · студент на адмінській), помилки входу (`domain`/`verify`/нове `oauth` замість 500) — теж там, з домашньої прибрано; open redirect закрито `safe_path()`/`safeNext()`; `deps.py` — 401 гостю, 403 не тій ролі; `api.js` на 401 → `/login?error=session&next=<поточна>`; кнопки навбара несуть поточну сторінку. Смоук `smoke_auth.py` **16/16**, `smoke_games.py` 70/70, build чистий.

- 2026-09-20 — **дев: посилання з бота на 127.0.0.1** (Vitalik: «якщо 127.0.0.1 — Safari не відкриває, якщо localhost — у Telegram посилання не робиться»): Telegram не лінкує хости без домену, а Vite слухав лише `[::1]:5030` (Node резолвить `localhost` у `::1`) → `vite.config.js` `host: '127.0.0.1'` (на `localhost` браузери перепадають на IPv4 самі), дев-дефолт `site_url` і `.env.example` — `http://127.0.0.1:5030`; нюанс сесій — у README [platform](platform/README.md). Vitalik додав лінк і на слово «сайті» в `bot/texts.py`.

- 2026-09-20 — **інструкція до бота** (Vitalik: «треба README щодо користування боту, які команди що роблять»): `app/api/bot/README.md` — запуск, сторона студента, команди адміна (`/here`, `/mute`), таблиця видів алертів з прикладами, що не алертиться, мапа коду; лінк з `app/README.md`. Страховку `caffeinate -t 6h` у `awake.sh` замінено на `-w <pid claude>` (помирає з сесією).

- 2026-09-19 — **алерти, друга хвиля** (Vitalik у чаті: «можеш реалізувати інші ідеї, які запропонував» — T112 Q3; звіт — [T113](tgbot/.t/T113--bot-alerts-2/report.md)): види `claim` (заявки), `game` (картка гри, обʼєкти, правила реченням як на сайті), `error` (винятки API через `add_exception_handler` і бота через `dp.errors`, з «нашим» кадром трейсбека), `digest` (09:00 Київ, по групах без ПІБ/GitHub/бота, цикл у процесі бота); `login` — кожен перший вхід через нове `User.seen_at`; `/mute <види>` і ключі видів у `/here`; `bot/game_alerts.py`, `bot/errors.py`, `bot/digest.py`, хуки `BackgroundTasks` у `my_claims`/`my_game`/`my_parts`/`my_rules`. Смоуки `smoke_events.py` **18/18**, `smoke_notify.py` **25/25**. Окремо, поза репо: хуки Claude Code `UserPromptSubmit`/`Stop`/`SessionEnd` → `dev-md-rules/scripts/awake.sh` — `caffeinate -is` лише поки агент працює (Mac засинав посеред роботи).

- 2026-09-19 — **адмін-алерти про зміни профілю в Telegram** (Vitalik у чаті: «всі зміни в профілі — мені у системний чат, нема чата — особисто; окремий чат або гілка форуму; на різні види… раз-раз, і налаштував»; звіт — [T111](tgbot/.t/T111--bot-alerts/report.md)): `record()` повертає diff, з нього `bot/alerts.py` будує повідомлення в стилі T109 (шапка 🟢/🟠/🔴 «Профіль · імʼя-лінк · група», рядок на поле, ✖️ для стертого); види `fill` / `change` / `login` у `KINDS` (`bot/notify.py`), маршрут `Route` у Mongo, привʼязка командою `/here` у чаті або гілці (`bot/here.py`, лише адмін із привʼязаним ботом), без маршруту чи при помилці доставки — адмінам особисто; хуки в профілі, відвʼязці, вході нового юзера (`BackgroundTasks`) і в `bind()`/`sync()` бота; `start.py` — лише приватні чати. Смоук `tests/smoke_notify.py` **21/21**. Питання — [T112](tgbot/.t/T112-Q--bot-alerts-questions.md).

- 2026-09-19 — **бот: повідомлення в стилі Vitalik** (три зразки в чаті: «👋 Привіт, Віталій! / ✔️ Дякую, записав тебе)», інтро з лінком на профіль, «вже було записано раніше»; стиль зафіксовано — [T109](tgbot/.t/T109-C--bot-message-style.md)): тексти винесено в `bot/texts.py`, HTML-розмітка через `DefaultBotProperties(parse_mode="HTML")`, імʼя — `html.quote`; `link.py` → `by_token()` + `bind(user, …)`, `sync()` віддає юзера хендлерам через `data["user"]`; нові гілки: той самий чат знову · перепривʼязка з іншого чату («🔁») · застаріле посилання («❌») · підказка про відсутній `@username` («☝️») · «далі буде» для привʼязаних; `SITE_URL` у конфігу (дев `http://localhost:5030`, `.env.example` — `https://python.labs.in.ua`); підказка в профілі — нове формулювання. Смоук бота **12/12**. Питання про помилкову привʼязку й відвʼязку в боті — [T110](tgbot/.t/T110-Q--bot-link-edge-cases.md) (рекомендації: відмовляти в перепривʼязці до відвʼязки на сайті; відвʼязка — лише на сайті).

- 2026-09-19 — **профіль v1.1: усі ідеї T108 + відповіді T107** (Vitalik у чаті: «давай зробимо всі ідеї з 108», GitHub-підказку лишати завжди): `UserHead.vue` — шапка з аватаром Google (або кружечок з літерою), `група · пошта` і чеклістом заповненості `✓ ПІБ · ○ GitHub · ✓ Telegram-бот` (на `/profile` і в адмінському `/students/:nick/edit`); `useForm()` — попередження про незбережені зміни (`onBeforeRouteLeave` + `beforeunload`), помилка зникає при першому редагуванні; `ProfileForm` — `pattern` на GitHub-URL (той самий регексп, браузер підкаже до відправки) + «відкрити ↗» біля заповненого поля, `autocomplete="off"` на ПІБ, підказки ПІБ/Telegram ховаються після заповнення (GitHub — завжди). Бот за [T107](tgbot/.t/T107-Q--tg-username-field.md): поле ніка лишається (Q1 b — сигнал, що важливе), `bind()`/`sync()` дзеркалять `@username` з Telegram, включно зі стиранням (Q2 a); смоук бота **9/9**, `npm run build` чистий; клік-тест — Vitalik.

- 2026-09-19 — **профіль: «Зберегти» лише при змінах, статус праворуч** (Vitalik у чаті: «кнопка неактивна на початку і стає активною лише якщо є зміни… "Збережено" справа, не на весь рядок»): `form.js` — `useForm()` (форма + знімок серверних даних, `dirty` = різниця, `fill()` після відповіді сервера — тому нормалізовані значення, обрізані `@` і `/`, одразу видно в полях); `ProfileForm.vue` — кнопка `disabled` без змін, «✓ Збережено» і помилка — дрібним текстом праворуч від кнопки з fade; `/profile` і `/students/:nick/edit` без власних `saved`/`error`. `npm run build` чистий; клік-тест — Vitalik (Chrome-розширення не відповідало). Ідеї далі — [T108](platform/.t/T108-Q--profile-polish.md).

- 2026-09-19 — **привʼязка бота: слідкування за ніком, авто-детект, відвʼязка** (питання Vitalik у чаті): бот оновлює `tg_username` з кожного повідомлення привʼязаного чату (middleware, `sync()` у `bot/link.py`); профіль після кліку опитує `/api/me` (2 с, до 3 хв) і сам показує «✅ Бот привʼязаний»; «відвʼязати» → `DELETE /api/me/telegram` (стирає `chat_id` + нік, обертає токен). Підказка описує обидва випадки deep link-у (у новому чаті — кнопка **Start**, у наявному клієнт шле `/start <токен>` сам; tdesktop: `!history->isEmpty()`), нюанс — у README [tgbot](tgbot/README.md). Смоук бота **8/8**, unlink перевірено ад-хок TestClient-ом. Чи лишати ручне поле ніка — [T107](tgbot/.t/T107-Q--tg-username-field.md).

- 2026-09-19 — **Telegram-бот: пустишка** (Vitalik у чаті: «давай реалізуємо Telegram-бота, поки що просто майже пустишка»; звіт — [T106](tgbot/.t/T106--tgbot-stub/report.md)): `app/api/bot/` на aiogram 3 (спільні `config`/`db`/`models` з API, тому не окремий пакет), запуск `uv run python -m bot` (long polling); `/start <payload>` виконує контракт привʼязки [T58](tgbot/.t/T58-C--tg-link-binding.md) — `tg_chat_id` + справжній `tg_username` через `record()` (actor `tgbot`), невідомий токен → відсилає до кнопки в профілі, голий `/start` і решта тексту — підказки; `TG_BOT_TOKEN` у конфігу (`.env.example`); смоук `tests/smoke_bot.py` **7/7** на заглушці повідомлення без Telegram. Vitalik: токен у `.env` → запуск → «Привʼязати бота» у профілі.

- 2026-09-19 — **тимчасовий замок сторінок** (Vitalik у чаті: «всі сторінки й меню невидимі для всіх; студентам поки лише Профіль»): у `router.js` кожен маршрут отримав `meta.access` (`admin` · `active` · нема = публічний) + guard, що відкидає чужих на `/`; `NavBar.vue` будує меню з тих самих `meta` через `router.resolve` — сторінка і пункт відкриваються разом; `user.js` — `userLoaded` (guard чекає `/api/me`) і `canAccess()`. Зараз студентам видно лише `/` і `/profile`, гостям — лише `/`; каталоги `/games`+`/tasks` для гостей тим самим закриті. Бекенд не чіпано (студенту API відповідає як раніше) — питання [T105](platform/.t/T105-Q--access-lock.md) Q1; порядок відкриття сторінок — Q2. `npm run build` чистий; клік-тест студентом/гостем — Vitalik.

- 2026-08-19 — **лудопедія: слоти параметрів (E2+E4), драбина E1–E4 закрита** (запит Vitalik у чаті «рухаємось далі»; звіт — [T103](platform/.t/T103--ludopedia-slots/report.md)): `Task.slots` (спека `{key, label, type: int|choice|bool|text, options?, unit?, required?}`) + `Claim.params` з валідацією по слотах (невідомий ключ / відсутній обовʼязковий / не той тип / choice поза options → 422); `SlotFields.vue` + `ClaimPicker` з міні-формою параметрів + параметри текстом у чіпах (`ClaimRow`, паспорт) — «Спавн за розкладом · Кожні 20 тіків · Макс. 4»; картка каталогу показує «⚙️ параметри»; **сид E4** — слоти на 20 картках (спавн/бій/стан/смерть/поразка/перемога/хвилі/суперник/генерація: int+unit, choice dfs/prim, bool «фінальна хвиля», text «цикл ходів») через YAML-канал повним колом (dict-fallback тепер і для slots; export→правка→import→export, roundtrip стабільний). Смоук **70/70**, build чистий. Адмін-форма слоти не редагує — канал v1 лише YAML. Окремо — **[T104](games/.t/T104-C--ludopedia-d3-d6.md): Д3–Д6 людською мовою** (запит «не зрозумів, що таке Д3»): приклади на картках → еталонні описи → осі варіацій → жнива, кожен крок на картці «Погоня»; Д3 пропонується наступним шматком після реакції.

- 2026-08-18 — **лудопедія: перший шматок коду** (Vitalik у чаті: «давай почнемо будувати», рішення старту — [T100](.rounds/.t/T100-R--ludopedia-kickoff.md): T99 «в цілому» + T97 за рекомендаціями як робоча гіпотеза; звіт — [T101](platform/.t/T101--ludopedia-core/report.md)): **E1 сутності** — `Part kind="entity"` + `role` (🧍👾📦💎🧱), `EntityCard` у `/my/game` і на паспорті, заявки механік прямо на сутностях; **правила-речення** — модель `Rule` (`when·cond·then·note`, словники в коді: тригери contact/timer, 15 ефектів), `/api/my/game/rules`, `RuleRow.vue` — «КОЛИ 🧍Гравець × 👾Ворог → ТО шкода 1 · зникає Б» селектами без синтаксису, каскад «частина зносить свої правила»; **Д2 інбокс знахідок** — модель `Ref` (+поля `status`/`task` на виріст до Д3), `/api/refs`, сторінка `/refs` «Знахідки» з YouTube-превʼю — спільна стрічка курсу, кидати лінки можуть усі активні. Смоук **60/60**, `npm run build` чистий. Рішення за замовчуванням — [T102](games/.t/T102-Q--ludopedia-build-questions.md) (видимість знахідок, словники v1, монетки за правила, назва). Далі: E2 слоти + E4 топ-20 · Д3 приклади на картках.

- 2026-08-17 — **лудопедія — north star мрії** (брейндамп-голос Vitalik у чаті: система не лише для студентів — агрегувати ігрове розмаїття світу з гранулярністю варіацій; «рівень 3 потрібен, але формочками, не синтаксисом»): концепт [T98](games/.t/T98-B--ludopedia.md) — огляд світу (Ludii/лудеми, VGDL, Machinations, паттерн-вікі; виконуваність ⊥ «все-все» → беремо композиційність без синтаксису), ядро з 4 механізмів (словник-картки · морфологія сутності+слоти · граматика «КОЛИ→ЯКЩО→ТО» · корпус прикладів+осі варіацій), принцип «опис гри ≠ робота студента» (Part/Claim-шари вже так розділені; референс-ігри отримують Part-шар, еталонні описи = меню студентам), межі чесно (Baba Is You, міра покриття, правило трьох), мапа Д0–Д6 (інбокс референсів → приклади на картках → еталонні описи → осі → жнива). Питання — [T99](games/.t/T99-Q--ludopedia-questions.md) (6: readback, ядро, розділення, `Rule` замість таблиці одразу, перший крок, студенти-співавтори корпусу). Ієрархія гранулярності: слот = число екземпляра · дитина сімʼї = зміна суті/ціни · вісь = енциклопедичний вимір.

- 2026-08-17 — **сесія механік (етап E, запит T86 Q1c)**: глибокий прохід драбини формальності на наскрізних Танчиках — рівні 0 (картки+текст, працює), 1 (слоти параметрів у картках: `spawn-schedule {кожні, макс}`…), 2 (таблиця взаємодій «X×Y→наслідок» зі словником-vocab; граф вікон — та сама філософія), 2.5 (стани сутностей — тримаємо в кишені), 3 (DSL — розібрано ще раз і знову «ні»: подвійний опис гри, парсер назавжди, унікальне однаково в текст) + місток до старих «4 варіантів» і монетна чесність — [T95](games/.t/T95-B--mechanics-deep-dive.md); модель реалізації без нової архітектури (сутності = `Part kind=entity`+`role`, `Task.slots`+`Claim.params`, `Interaction`-рядки, кроки E1–E4) — [T96](platform/.t/T96-P--entities-slots-model.md); рішення — [T97](games/.t/T97-Q--mechanics-session-questions.md) (5 питань: драбина, сутності зараз, монетки за рядки, словник role, тіки/секунди).

- 2026-08-17 — **етап D: граф переходів і фініш паспорта** ([T94](platform/.t/T94--passport-graph/report.md)): `GameGraph.vue` — SVG-карта без залежностей (вузли-вікна з мініатюрами скриншотів, стрілки з пунктів меню з мітками, злиття паралельних ребер, BFS-шари зліва направо, клік-скрол до картки вікна, підказка за відсутності ребер) — на паспорті `/students/<nick>` і в редакторі `/my/game`; у шапці паспорта — монетки заявленого за типами (👑×N…, довідково без зарахування). Фронт-онлі: смоук 43/43 не чіпався, `npm run build` чистий. Пілот «Моя гра» A–D закрито → великий клік-тест Vitalik.

- 2026-08-16 — **етап C: перейменування + фронт «Моєї гри»** ([T93](platform/.t/T93--my-game-front/report.md); рішення [T92](platform/.t/T92-Q--naming-urls.md) — всі «a» в чаті + кошик): моделі `Game`/`Part`/`Claim` (каталожна → `BaseGame`; колекції `student_games`/`game_parts`/`claims`, було порожньо — без міграцій); API `/api/my/game…` + `/api/my/claims` + `/api/students` (галерея role-shaped, паспорт по nick); сторінки `/my/game` (редактор: вікна, меню, заявки, скриншоти, 💡-пари елемент→функція), `/students` (картки + адмін-таблиця перемикачем), `/students/<nick>` (паспорт), edit по nick; кошик `uploads.trash/` + каскадні видалення через `record_delete` — ніщо не зникає; смоук **43/43** (`tests/smoke_games.py`), `npm run build` чистий. Клік-тест і активація `window`-карток — Vitalik. Паралельно: відповіді [T08](grading/.t/T08-Q--grading-open-questions.md) Q1–Q2 зафіксовано в [grading/README](grading/README.md) (бали лише ростуть; захистів — мінімум, лише 80+) — раунд оцінювання можна збирати.

- 2026-08-16 — **питання неймінгу і URL перед фронтом** ([T92](platform/.t/T92-Q--naming-urls.md)): Vitalik просить переглянути `Work`/`WorkObject`/`Claim` і продумати URL логічно — варіанти з рекомендаціями (`Game`+`BaseGame` · `Part` · лишити `Claim` · «сторінка студента = паспорт гри» `/students/<nick>`); момент ідеальний — живі колекції порожні, перейменування без міграцій. Дрібне: у `CLAUDE.md` нове правило «пропонуй коміт-меседж після пакета змін»; підтверджено — `tasks.yaml` вже в живій базі (209 карток, 13 `window`); нюанс видалень: у Mongo все лишається в history, але файли скриншотів стираються з диска назавжди (логуються лише імена) — за бажанням додамо «кошик» `.trash`.

- 2026-08-16 — **бекенд «Моєї гри» v1 (етап B)** ([T91](platform/.t/T91--my-game-backend/report.md)) за дизайном [T90](platform/.t/T90-P--my-game-v1-design.md) і рішеннями [T89](platform/.t/T89-Q--my-game-questions.md) (одна гра · видимість залогіненим · основа з каталогу або своя): моделі `Work`/`WorkObject`/`Claim`, роути `/api/works` (галерея, паспорт зі збагаченням карток, обʼєкти з авто-заявкою типу і каскадним видаленням, заявки з дедупом), скриншоти `uploads/` (8 МБ, StaticFiles, git-ignored), `record_delete()` в history; смоук 35/35 (`app/api/tests/smoke_works.py`, скретч-база). Фронт — етап C; Vitalik: активувати `window`-картки після ревізії.

- 2026-08-15 — **пілот «Моя гра»: рішення + етап A** ([T87](.rounds/.t/T87-R--game-round-decisions.md)): обʼєктна модель [T83](reports/.t/T83-B--game-objects-model.md) прийнята повністю (T84 всі «a»), механіки — окрема сесія (T86 Q1c), план етапів A–E; **сид обʼєктного раунду** ([T88](tasks/.t/T88--catalog-objects-seed/report.md)): +24 чернетки → 209 карток / 10 сімей — 7 типів вікон + тег `window` на 13 картках, сімʼя елементів налаштувань, sound-volume, діти редактора (5) і магазину (4); урок YAML «`#` без лапок = коментар» — памʼятка в tasks/README. Питання етапу B — [T89](platform/.t/T89-Q--my-game-questions.md) (ігор на студента, видимість, гра-основа).

- 2026-08-15 — **раунд «опис гри студента»** (брейндамп Vitalik — [T82](.rounds/.t/T82-R--game-desc-braindump.md); GPT-чати-натхнення недоступні без браузера): пропозиція обʼєктної моделі «каталог → обʼєкти гри → заявки» — вікна/меню як якорі заявок і скриншотів, докази `none/screenshot/gif/link` атрибутом картки, GitHub-гібрид з нормалізацією в коміт-пін, граф переходів вікон, план «Моєї гри» v1 ([T83](reports/.t/T83-B--game-objects-model.md)); драбина формальності механік 0→1→2 без DSL — слоти параметрів на картках, таблиця взаємодій «X×Y→наслідок» зі словником, редактор/магазин через сімʼї ([T85](games/.t/T85-B--mechanics-formalization.md)). Питання: [T84](reports/.t/T84-Q--game-objects-questions.md) (8, відкривають «Мою гру» v1) + [T86](games/.t/T86-Q--mechanics-questions.md) (4).

- 2026-08-10 — **порти дев-серверів: python-labs = №3** ([T81](platform/.t/T81-C--dev-ports.md)): крос-проєктна схема «проєкт №N → API `8000+10·N`, Vite `5000+10·N`», реєстр номерів — у `dev-md-rules/DEV.md` (нова секція там); тут — API **8030**, фронт **5030**: `vite.config.js` (`strictPort`, proxy → 8030) + `app/README.md` (`fastapi dev --port 8030`); робоча адреса тепер http://localhost:5030. Vitalik: додати нові redirect URI в GCP (кроки в T81) і `--port 8030` у PyCharm-конфігурації.

- 2026-08-06 — **каталог: сімʼї v2 + YAML v2 + активація** (відповіді [T78](platform/.t/T78-Q--catalog-ui2.md) + чат): модель варіантів → **батько-діти** (`Task.parent`, один рівень; діти = повноцінні завдання з описом/статусом/історією; міграція 23 варіантів у документи, `variants` знято звідусіль; UI рекурсивний — сімʼя розгортається в білий блок із звичайними картками; обґрунтування — [T79](platform/.t/T79-C--family-v2.md)); **YAML v2** за синтаксисом Vitalik (T78 Q3): дерево зона→підзона, рядок `🥉 Назва [ігри] ⭐ ✍️ — опис`, позиція = order, `children:` вкладено (`taskline.py` + переписаний `catalog_io.py`, раундтрип «без змін 185»); **активовано 74 впевнені картки** (code+support повністю + класика) — гість уже бачить каталог; косметика карток: ✏️ на рядку опису, бейдж чернетки з відступом, сімейний ×N — сірий бейдж справа, діапазон «📎‥🥇» з сірими крапками, тег гри — лінк на гру, «＋» підзони помітніший; кольори: брейншторм v2 — ємність сімейств і розкладка «зона = діапазон сімейства» ([T80](platform/.t/T80-B--colors-plan.md)) + сторінка-пропозиція **`/colors2`** (`/colors` лишилась). Зони: рішення T78 Q1 — **лишаємо 2 зони** (Сутності/Логіка) + гібрид сутностей.

- 2026-08-06 — **каталог: UI-ітерація 2** (відповіді [T74](platform/.t/T74-Q--catalog-visuals.md) + чат): multicol → власний **masonry** без залежностей (`MasonryGroups.vue`: жадібна розкладка груп-підзон по колонках, розгортання картки більше нічого не пересуває; пояснення підходів — [T75](platform/.t/T75-C--masonry-layouts.md)); монетки **👑🥇🥈🥉📎🌱** (корона = верх шкали замість «золото×2»; єдиний такий таск `unique-puzzle-generation` переведено в базі); відтінки підзон: graded → **сусідні хʼю** ±12°; сімʼя — сірий `×N` + діапазон цін варіантів «📎–🥇» (T74 Q6); бейджі ⭐/чернетка/×N — інлайн після назви; прибрано «універсальне», ✏️ без тексту; **гостьовий доступ** до `/games`+`/tasks` (GET публічні, гість бачить лише active; NavBar для всіх); сторінка гри = той самий каталог (`TaskCatalog.vue`, лише специфічні завдання; універсальні — окремим пунктом фільтра «🌍 Універсальні»); «＋» у шапці підзони (адмін, префіл зони/підзони у формі); майстерня кольорів **`/colors`** (сімейства R…M, принципи h/l/s, набори К1–К3); «Всі зони»→«Всі», короткі назви зон, лічильники тонші. Нове думання: зони — аналіз 7 років категорій ([T76](tasks/.t/T76-B--zones-rethink.md)), людяний YAML-формат ([T77](platform/.t/T77-P--yaml-tree-format.md)); рішення — [T78](platform/.t/T78-Q--catalog-ui2.md). `npm run build` чистий, guest-смоук 200/200/200.

- 2026-08-05 — **каталог: UI-ітерація за фідбеком чату**: дерево зон зліва → верхня дворядкова навігація (кнопки зон + чіпи підзон під обраною); стрічка `/tasks` → багатоколонкова (CSS multicol `columns: 20rem`, підзона = нерозривний блок; аналіз підходів — [T72](platform/.t/T72-B--catalog-columns.md)) на всю ширину екрана (route meta `wide`); іконки всім зонам і 38 підзонам + кольори зон із graded-відтінками підзон (`zones.py`: subzone = кортеж `(title, icon)`, зона + `icon`/`color`; брейншторм-підтвердження — [T74](platform/.t/T74-Q--catalog-visuals.md)); монетки: 🥇🥈🥉 лишаються, деревʼяна 🪵→🌿, оловʼяна ⚪→🔵; сімʼї помітніші (тінь-«стопка», бейдж ×N, у шапці монетки варіантів замість довідкової ціни картки, варіанти як міні-картки) — концепт пояснено в [T73](tasks/.t/T73-C--task-families.md); чернетки навпаки приглушені (пунктирна рамка + сірий заголовок замість помаранчевого бейджа), архів — напівпрозорий із закресленням. `npm run build` чистий; клік-тест — Vitalik.
- 2026-08-05 — **каталог v1 реалізовано** ([T71](platform/.t/T71--catalog-v1/report.md)) за рішеннями [T70](.rounds/.t/T70-R--catalog-decisions.md) (T69 повністю + T37/T06: фасети, сімʼї, algo-тег, універсальність, без множників якості; зафіксовані напрями — деградуючі ціни повторів і inline-редагування): моделі `Game`/`Task` + `zones.py`, роути GET/POST/PUT з `record()`-аудитом, `catalog_io.py` export/import YAML ↔ Mongo; фронт — `/games` (галерея), `/games/:slug` (осі + завдання гри), `/tasks` (дерево зон + пошук + чіпи, стан у URL), адмін-форми; сид з wiki-ядра: **17 ігор + 162 завдання** (7 сімей, 16 золотих, ціни орієнтовні, все чернетками) — залито в живу базу; смоук 17/17 + наскрізний HTTP-тест (студент 9/0, адмін 17/162), `npm run build` чистий. Нові залежності: pyyaml, marked.
- 2026-08-04 — раунд «Каталог v1: ігри + завдання» (проєктування, код — після відповідей): readback рамок ([T65](.rounds/.t/T65-R--catalog-round-readback.md)); брейншторм зберігання — Mongo / git-файли / гібрид з YAML-знімком, рекомендація «правда в Mongo, сид = перший import» ([T66](platform/.t/T66-B--catalog-storage.md)); брейншторм подачі — весь каталог одним JSON на клієнт, дерево зон + стрічка з розгортанням + пошук/чіпи, ігри галереєю ([T67](platform/.t/T67-B--catalog-ui.md)); зведена пропозиція v1 — моделі Game/Task (`max_count` для повторюваності, `variants` для сімей), API, сторінки, сид з wiki-ядра ([T68](platform/.t/T68-P--catalog-v1-design.md)). Питання — [T69](platform/.t/T69-Q--catalog-v1-questions.md) (7 шт.) + передумови [T37](tasks/.t/T37-Q--taxonomy-questions.md)/[T06](tasks/.t/T06-Q--tasks-open-questions.md).
- 2026-08-04 — фікс імпорту (баг: усі студенти падали в першу групу, а `cist.txt` містить 7 шапок «Список групи …»): парсер тепер перемикає поточну групу по шапках; семантика імпорту — **оновлення замість пропуску**: наявний студент (по пошті) отримує свіжу групу, ПІБ заповнюється лише в порожні поля (самоперейменування студента важливіше за ЦІСТ — рішення чату), решта внесеного не чіпається; звіт «додано/оновлено/без змін». Смоук 12/12. Рішення чату: **CSS лишається Bootstrap 5** ([T63](platform/.t/T63-Q--css-framework-choice.md) закрито відповіддю в чаті після порівняння [T62](platform/.t/T62-C--css-frameworks.md)).
- 2026-08-04 — **профіль v1 реалізовано** ([T64](platform/.t/T64--profile-v1/report.md)) за рішеннями [T61](.rounds/.t/T61-R--profile-decisions.md): `/profile` (картки ПІБ/GitHub/Telegram, інструкції, кнопка привʼязки `@python_nure_bot` deep link-ом з токеном), `/students` + `/students/:id` (адмін: таблиця, textarea-імпорт формату ЦІСТ з автогрупою, редагування тією ж формою), колекція `history` з диффами (створення + правки). Смоук: 15 перевірок на тест-базі (108 студентів з `cist.txt`), `npm run build` чистий; клік-тест — за Vitalik. Q1 (CSS) відкладено: порівняння Bootstrap/Tailwind — [T62](platform/.t/T62-C--css-frameworks.md), вибір — [T63](platform/.t/T63-Q--css-framework-choice.md), v1 на Bootstrap.
- 2026-08-04 — раунд «профіль студента» (перша студентська сторінка): readback вводу ([T55](.rounds/.t/T55-R--profile-round-readback.md)); дизайн `/profile` + `/students` з масовим імпортом ([T56](platform/.t/T56-P--profile-pages-design.md)); історичність — порівняння варіантів, рекомендація «одна колекція `history` з диффами» ([T57](platform/.t/T57-B--history-storage.md)); привʼязка Telegram через deep link із випадковим токеном, без пошти в URL ([T58](tgbot/.t/T58-C--tg-link-binding.md)); ескіз колекцій майбутньої системи ([T59](platform/.t/T59-C--data-model-sketch.md)). Питання раунду — [T60](platform/.t/T60-Q--profile-questions.md) (9 шт., головні: Bootstrap vs Tailwind, одне чи кілька GitHub-полів, чи реєструвати бота вже). Код — після відповідей.
- 2026-08-03 — **`data/wiki/` створено і наповнено** за рішеннями [T54](.rounds/.t/T54-R--kb-round-decisions.md) (T52/T53: імʼя `wiki` замість kb, каркас T51 прийнято, всі 4 класи ігор рівноправні, фігурні = hot-seat + AI-золото, пазли = плеєр + генератор/розвʼязувач-золото): `games/` — 17 чернеток (девʼятка з v2-Notion 🌿 + шортліст T32 💭 + парасолі chess/paper-puzzles 💭) + карта класів; `tasks/` — 6 зон з курованим ядром (~150 пунктів) + карта зон + staging `_v3-raw.md` (2019 унікальних назв v3 по зонах; разовий скрипт — не в репо, бо стежка одноразова).
- 2026-08-02 — раунд «база знань контенту»: простір ігор розширено 4 класами за актором — аватарні/курсорні/фігурні/пазли-розмітки + осі опису ([T49](games/.t/T49-B--game-classes.md)); зони таксономії узагальнено на нові класи ([T50](tasks/.t/T50-B--kb-task-groups.md)); спроєктовано `data/kb/` — вільний Markdown-простір «ігри + групи завдань» з форматами файлів і граматикою пунктів ([T51](.t/T51-P--kb-structure.md)); питання — [T52](.t/T52-Q--kb-questions.md) (структура/старт) і [T53](games/.t/T53-Q--game-classes-questions.md) (статус класів). Наповнення — після відповідей.
- 2026-07-26 — дослідження доступу до студентських репо через колаборатора ([T48](github/.t/T48-C--collaborator-access.md)): так, працює — запрошення приймається через API, репо клонуються токеном; варіанти авторизації (classic/fine-grained PAT, бот, GitHub App, deploy keys) і головний ризик — blast radius токена на сервері; рішення T16 Q1 усе ще за Vitalik.
- 2026-07-26 — 🎉 **реальний Google-вхід підтверджено** (Vitalik увійшов з @nure.ua) — мікро-MVP закрито повністю; додано `prompt=select_account` (Google щоразу показує вибір акаунта — важливо для спільних лаб-компів).
- 2026-07-26 — креди Google в `.env`, бекенд перезапущено; БД `labs` → `python_labs` (стару видалено); **фікс OAuth-пастки**: Vite-proxy тепер зберігає Host (`changeOrigin: false`), інакше `redirect_uri` ставав `127.0.0.1:8000` і Google давав `redirect_uri_mismatch`; PyCharm-інструкція — [T47](platform/.t/T47-C--pycharm-setup.md); Dropbox — повний синк за преференцією Vitalik (пропозицію виключень знято).
- 2026-07-26 — **мікро-MVP реалізовано** ([T44](platform/.t/T44--auth-mvp/report.md)): `app/api/` (FastAPI + Beanie + Authlib) + `app/vue/` (Vue 3 + Bootstrap 5, Vite) + кореневий `CLAUDE.md` з правилами код-стайлу; dev-цикл перевірено наскрізно (dev-вхід → Mongo → сесія → logout), реальний Google-вхід чекає кредів у `.env`. Рішення [T46](platform/.t/T46-Q--pre-code-questions.md): назва «Python Labs», dev-вхід є, PyCharm Pro. Нюанс: `localhost:8000` по IPv6 перехоплює Docker Desktop → всюди явний `127.0.0.1`.
- 2026-07-25 — Beanie затверджено остаточно (чат); теки коду — `app/api/` + `app/vue/`; нове правило код-стайлу «дрібні файли, ≤ ~80 рядків»; детальний каркас мікро-MVP по файлах — [T45](platform/.t/T45-P--app-skeleton.md), останні уточнення перед кодом — [T46](platform/.t/T46-Q--pre-code-questions.md).
- 2026-07-25 — рішення по T40 ([T41](.rounds/.t/T41-R--auth-mvp-decisions.md)): **стек затверджено** — FastAPI + MongoDB + Vue 3 + Bootstrap 5, SPA одразу, код у `app/` цього репо, поки localhost, mongo і прод без docker; Beanie обґрунтовано ([T42](platform/.t/T42-C--beanie-vs-alternatives.md)), інструкція Google OAuth ([T43](platform/.t/T43-C--gcp-oauth-setup.md)), план задачі мікро-MVP ([T44](platform/.t/T44--auth-mvp/plan.md)).
- 2026-07-25 — курс на «чергу 0.5»: мікро-MVP «сторінка + Google OAuth лише @nure.ua» ([T38](.rounds/.t/T38-R--auth-mvp-readback.md)); перегляд стеку під тверде бажання Mongo — пропозиція FastAPI + MongoDB (Beanie) + Vue 3 замість Django+Postgres з T21 ([T39](platform/.t/T39-B--stack-rethink.md)); питання мікро-MVP — [T40](platform/.t/T40-Q--auth-mvp-questions.md) (Q1 — нова редакція T22 Q1).
- 2026-07-19 — розбір контенту: аналіз v3-даних (7 років категорій) + 9 ігор → ресерч-каталог ~40 grid-ігор у 7 сімʼях ([T32](games/.t/T32-C--grid-games-catalog.md)), фасетна таксономія тасків «дім + теги» ([T33](tasks/.t/T33-B--task-taxonomy.md)), декларативна spec-модель гри як сума обраних тасків ([T34](games/.t/T34-P--game-spec-model.md)), 10 несподіваних ідей ([T35](.t/T35-B--unexpected-ideas.md)); питання — [T36](games/.t/T36-Q--games-catalog-questions.md), [T37](tasks/.t/T37-Q--taxonomy-questions.md).
- 2026-07-19 — `data/v3/`: всі старі матеріали в єдиній 15-колонковій CSV-схемі ([T27](tasks/.t/T27-P--v3-csv-schema.md), рішення [T28](tasks/.t/T28-Q--v3-csv-questions.md)): 7 файлів Coda (props влиті, 4 сміттєві колонки викинуто) + 2 файли з Notion-md; генерація і звірка без втрат — скрипти `data/scripts/` ([T31](tasks/.t/T31--v3-conversion/report.md)). Перед цим у v2 вичищені аномалії експорту ([T29](tasks/.t/T29-C--v2-data-anomalies.md)): биті рядки, 17+ порожніх заготовок (руками Vitalik + доправки).
- 2026-07-19 — `data/v2/`: очищена копія старих матеріалів з `data/v1/` (T26 Q5). CSV (coda): багаторядкові клітинки склеєні в один рядок через літеральний `\n` — кожен запис тепер на одному фізичному рядку (91 клітинка у tasks-20, ~230 загалом); дані полів не змінені (лише trailing-пробіли рядків). MD (notion): прибрані посилання на notion-сторінки (`notion.so`, `notion://`, відносні `.md`-лінки експорту), tss.co.ua та coda.io (текст лінка скрізь збережено) і `<img>`-іконки callout'ів; збережені лише Wikipedia- та YouTube-посилання. Перевірено скриптом: поле-в-поле для CSV, слово-в-слово для MD.
- 2026-07-17 — старт проєкту: `dev init`; рідбек брейндампу [T01](.rounds/.t/T01-R--braindump-readback.md); 11 плоских вузлів за [T02](.t/T02-P--node-structure.md); по кожному вузлу C/B-аналіз + Q-тікет (T03–T24); стратегія MVP [T25](.t/T25-B--mvp-strategy.md), глобальні питання [T26](.t/T26-Q--roadmap-open-questions.md); README всіх вузлів — скелети (рішень ще нема).
