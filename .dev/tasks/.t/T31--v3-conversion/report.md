# T31 · Конвертація даних v2 → v3 (єдина CSV-схема) — звіт

`2026-07-19` · ⚙️ задача · завершено

## Зроблено

- Доправлені хвости ручної чистки v2 (після коміту `f3252af`): у tasks-19 злито відірваний шматок опису «Двери» (рядок «Ключ нужно собрать отдельно (инвентарь).» — та сама поломка експорту, що й у «Псевдографики») і прибрано зайвий хвіст `,,,,` зі злитого рядка «Псевдографика» (13 полів → 9); видалено останні безіменні рядки-заготовки: 1 у tasks-23, 5 у tasks-24.
- `data/scripts/convert_coda.py` — 10 v2-CSV → 7 файлів `data/v3/coda/pzpi-YY.csv`: єдина 15-колонкова схема ([T27](../T27-P--v3-csv-schema.md)), props влиті після tasks (`source`), 4 колонки викинуто за [T28](../T28-Q--v3-csv-questions.md) Q2, значення verbatim.
- `data/scripts/convert_notion.py` — `games/tasks.md` + `games/algo.md` → `data/v3/notion/{tasks,algo}.csv`: буліт = рядок, курсивне пояснення в дужках → `description`, вкладені пункти → `description` через літеральний `\n`, `###` → `category`, `##` → `supercategory` (tasks.md), lead-in рядки з `:` → `subcategory` (24 значення), описи категорій і «Основний задум» лишаються тільки в md.
- `data/scripts/check.py` — зворотна перевірка: Coda — проєкція v3 назад у порядок/склад колонок v2 і порівняння клітинка-в-клітинку; Notion — флеттен CSV проти флеттену md (нормалізація: без md-розмітки, зі згорнутими пробілами).

## Перевірено

- `check.py` — все зелене: 10/10 Coda-файлів ідентичні (211+234+454+119+51+131+75+137+92+408 рядків), Notion — 206 пунктів tasks.md і 24 пункти algo.md збігаються за текстом і порядком. Викинуті колонки перелічуються у звіті явно.
- Обсяг v3: Coda 1912 рядків у 7 файлах, Notion 206 + 12 рядків.

## Хвости

- Git: мої доправки v2 не закомічені; `data/v3/` і `data/scripts/` — нові, неперевірені в git (коміт — за Vitalik).
- [T30](../T30-Q--v3-anomaly-fixes.md) знято фактом ручної чистки (слоти лишились порожні — відповіді вже не потрібні).
- `notion/algo.csv`: назви на кшталт «Автоматична генерація цікавих рівнів, наприклад:» лишені verbatim (з хвостом «, наприклад:») — свідомо, за принципом «нічого не перейменовувати»; підчистити можна буде на етапі нової таксономії.
