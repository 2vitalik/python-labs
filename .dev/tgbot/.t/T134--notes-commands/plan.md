# T134 · Нотатки: /note видима, /hide тиха, guest-згадка — план

`2026-09-24` · ⚙️ задача

Контекст: відповіді Vitalik на [T133](../T133-Q--notes-choices.md) у чаті — Q1: `/note` для видимих і `/hide` для тихих нотаток замість «📝»; питання, чи може бот писати від себе прямо в чаті зі студентом і чи видно, що це бот; Q2 a (поки алерти + Mongo); Q3 a (рядок про збереження). Guest Mode у BotFather не знайшов. Попередня версія — [T132](../T132--chat-automation-notes/report.md).

## План

1. Команди `/note` і `/hide` в усіх джерелах: чат зі студентом (Chat Automation), форум (ефемерно), чат з ботом, guest-згадка `@python_nure_bot /note …`.
2. Видима нотатка — найкращим доступним шляхом: у чаті зі студентом від імені викладача з маркером 🤖 (клієнти бота не позначають), не вдалося — особисто від бота; у форумі — відповідь у гілці; з чату з ботом — особисто; guest — від бота в тому ж чаті. Поле `Note.shown`.
3. Винести доставку в `bot/note_show.py` (файли ≤ ~80 рядків).
4. Смоук `smoke_notes.py`, README бота (команди, Guest Mode у міні-апці), README вузла, CHANGELOG.
