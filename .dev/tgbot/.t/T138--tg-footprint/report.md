# T138 · Слід студента в Telegram: членство, реакції, raw, дата привʼязки — звіт

`2026-09-25` · ⚙️ задача · план — [plan.md](plan.md)

## Зроблено

- `bot/log.py`: `event_row()` — спільний запис не-повідомлень; хендлери `chat_member` + `my_chat_member` (`kind=member`, `content_type=chat_member`, текст `old → new · invite · by`), `chat_join_request` (`join_request`, текст — bio), `message_reaction` (`kind=reaction`, текст — емодзі через пробіл, порожній = знято); `save()` пише `raw` для вхідних.
- `models/message.py`: `raw: dict`, коментарі до `kind`/`content_type`.
- `models/user.py`: `tg_linked_at` (+ в `api()`); `bot/link.py` — ставить при зміні `chat_id`; `routes/me.py` — `None` при відвʼязці.
- Смоуки: `smoke_log.py` **15/15** (вступ, запит на вступ, реакція додана/знята, `raw` є у вхідних і порожній у вихідних), `smoke_bot.py` **12/12** (дата привʼязки), `smoke_notes.py` **20/20**.
- Перевірено перед кодом: бот у форумі — адміністратор (тож `chat_member` і `message_reaction` приходять), конструктори aiogram для цих подій, розмір `raw` (0.3–0.5 KB на подію).

## Межі (Telegram ботам не дає)

- Хто прочитав, онлайн, набір тексту, дзвінки, перегляди повідомлень у групах; видалення в групах; історія форуму до вступу бота; реакції — лише у чатах, де бот адмін (форум), у бізнес-чатах — ні.

## Наживо

- Vitalik: хтось вступає у форум (або тестовий акаунт виходить-вступає) → рядок `member`; поставити реакцію на повідомлення у форумі → рядок `reaction` з емодзі, зняти — порожній; повторний `/start <токен>` з іншого чату → `tg_linked_at` оновлюється.
