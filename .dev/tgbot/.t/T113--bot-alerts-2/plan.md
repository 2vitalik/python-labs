# T113 · Алерти, друга хвиля: заявки, гра, помилки, дайджест, перший вхід, /mute — план

`2026-09-19` · ⚙️ задача · запит Vitalik у чаті після коміту T111: «можеш реалізувати інші ідеї, які запропонував» — тобто [T112](../T112-Q--bot-alerts-questions.md) Q3 a–e · звіт — [report.md](report.md)

Контекст: інфраструктура алертів з [T111](../T111--bot-alerts/report.md) (`KINDS`, `Route`, `/here`, `notify.send`); гра студента — `routes/my_game|my_parts|my_claims|my_rules.py`; речення правил на сайті — `app/vue/src/rules.js`.

## Рамки

- Новий вид — рядок у `KINDS` + виклик у маршруті: `claim` (заявки), `game` (картка гри, обʼєкти, правила), `error` (необроблені винятки API і бота), `digest` (ранковий підсумок незаповнених профілів); `login` розширюється на перший вхід імпортованих (нове поле `User.seen_at`).
- Гра — лише «створено / видалено» для обʼєктів і правил та зміни картки; правки обʼєктів, скриншоти, порядок — ні (шум).
- Шумні види треба вміти вимкнути без групи → `/mute <види>` (маршрут «нікуди»); `/here <види>` знімає.
- Помилки: API — `add_exception_handler(Exception)`, клієнту 500 одразу, алерт у `BackgroundTask` відповіді; бот — `dp.errors`. У тексті — перший рядок помилки й останній «наш» кадр трейсбека.
- Дайджест — цикл у процесі бота (09:00 Київ), мовчить, коли все заповнено.

## Кроки

1. `notify.py`: 7 видів, `label()` з ключем, `MUTED`; `here.py`: `/mute`, статус із ключами.
2. `alerts.py`: `event()`, `signed_in()` для всіх перших входів; `auth.py` + `User.seen_at`.
3. `game_alerts.py`: `claim()`, `game()`, `part()`, `rule()` + `sentence()` (словник ефектів дублює `rules.js`); хуки в 4 маршрутах.
4. `errors.py` (`api_handler`, `bot_handler`) → `main.py`, `__main__.py`; `digest.py` → `__main__.py`.
5. Смоук `tests/smoke_events.py`; правки `smoke_notify.py`; доки.
