# T135 · Нотатки лише для викладача: /note лишається, /hide зникає — план

`2026-09-24` · ⚙️ задача

Контекст: Vitalik у чаті після [T134](../T134--notes-commands/report.md): «нотатки — суто інформація для мене, щось, що повинно зберегтись у системі, щоб не забулось; писати щось з бота студенту не треба, згадувати студента — не треба; для `/note` просто залишити як є; 🤖 і 📝 не потрібні». Guest Mode увімкнено; Chat Automation підключено (Telegram Desktop → Business → Chatbots).

## План

1. Прибрати доставку: `note_show.py`, поле `Note.shown`, guest-обробку нотаток, 🤖/📝-префікси, DM студенту, відповіді в гілці форуму.
2. `/note` — лишається в чаті як написано; `/hide` — зникає з чату зі студентом (Chat Automation) і ефемерний у форумі. Обидві: запис у `notes` + алерт (📝 / 🙈), «✔️ Записав» лише тобі.
3. `Note.visible` → `hidden`. Реєстрація команд: ефемерна лише `hide`.
4. Смоук, README бота (команди, права — лише «Delete all messages»), README вузла, CHANGELOG.
