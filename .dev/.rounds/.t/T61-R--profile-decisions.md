# T61 · Рішення по T60: профіль v1

`2026-08-04` · ✔️ readback

Контекст:
- джерело — `>`-відповіді Vitalik у [T60](../../platform/.t/T60-Q--profile-questions.md) (2026-08-04);
- реалізація — задача [T64](../../platform/.t/T64--profile-v1/plan.md).

## Вирішено

- **Q2 — GitHub:** одне поле-URL; старі значення живуть в історії.
- **Q3 — Бот уже існує: `@python_nure_bot`** → `TG_BOT_NAME` у конфіг, кнопка привʼязки жива одразу.
- **Q4 — ПІБ:** три окремі поля; префіл з Google **лише в порожні поля** (у @nure.ua імена зазвичай якісні — беру `family_name`/`given_name` з OAuth-клеймів, по батькові лишається порожнім).
- **Q5 — Група:** так, поле `group`; сетиться імпортом.
- **Q6 — Історія:** одна глобальна колекція `history` з диффами ([T57](../../platform/.t/T57-B--history-storage.md), варіант a).
- **Q7 — Імпорт:** одразу, адаптований під реальний `data/students/cist.txt`: шапка «Список групи ПЗПІ-25-1 …» → назва групи регекспом; рядки `ПІБ ⇥ № залікової ⇥ контракт ⇥ email` → пошта регекспом, ПІБ — перше tab-поле без `@`, розбивається на прізвище/імʼя/по батькові за пробілами; рядки без пошти (номери, заголовки) ігноруються.
- **Q8 — Маршрут:** окремий `/profile` + лінк у NavBar.
- **Q9 — Інструкція:** «розшарити на `2vitalik`» як у Notion; бот-акаунт — після [T16](../../github/.t/T16-Q--github-open-questions.md) Q1.

## Q1 (CSS-фреймворк) — відкладено, не блокує

- Vitalik попросив порівняння Bootstrap vs Tailwind з прикладами коду → [T62](../../platform/.t/T62-C--css-frameworks.md); фінальний вибір — [T63](../../platform/.t/T63-Q--css-framework-choice.md).
- v1 реалізується на **Bootstrap 5** (статус-кво, вже в коді); якщо вибір упаде на Tailwind — мігрувати треба буде 4 невеликі vue-файли, це дешево.

## Твої думки та питання

> 
