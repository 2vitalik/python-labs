# T136 · Методичка закрита від студентів до завершення — звіт

`2026-09-25` · ⚙️ задача · план — [plan.md](plan.md)

Зроблено за планом. Смоук `tests/smoke_guide.py` **21/21** (початок переписано під новий доступ), `npm run build` чистий; headless Chrome гостем: `/` — кроки реєстрації, `/labs` · `/method` · `/games` — «Потрібен вхід». Живий API на 8030: `GET /api/guide` гостем → 401.

## Як тепер

- **API** `routes/guide.py`: `GET /api/guide` і `GET /api/guide/{slug}` — `admin_user` (гість 401, студент 403), як і решта роутів файла; окрема 404-перевірка чернетки для не-адмінів стала зайвою і прибрана. PUT / history / publish — без змін.
- **SPA** `router.js`: `const hidden = { access: 'admin' }` на `/labs`, `/labs/:n`, `/score`, `/howto`, `/changes`, `/method`, `/games`, `/tasks`. Меню будується з доступу маршрутів → студент бачить лише «Профіль», гість — тільки кнопку входу.
- **Домашня** `HomePage.vue`: адміну — текст `home` як був; гостю і студенту — новий `StartSteps.vue`: лід «одна гра на весь семестр», callout «🚧 Методичка ще пишеться й відкриється тут пізніше», три кроки з ✅/⬜ — увійти з `@nure.ua` → заповнити профіль (✅ коли є GitHub) → привʼязати бота (✅ коли `tg_linked`); лінки на `/my/profile` лише активним; гостю — кнопка «Увійти з Google», pending — «⏳ Тебе ще нема в списку курсу — доступ до профілю відкриє викладач». Рядок «· доступ надає викладач» біля привітання прибрано — тепер це каже сам блок.
- Не чіпав: `GuideHead` (гілка для не-адміна лишилась — це стан для повернення), `LoginPage` («Лише для викладача» на закритій сторінці — правда), тексти бота (лінкують лише `/my/profile` і сайт).

## Як відкрити назад

- `router.js`: `const hidden = {}` (або прибрати `...hidden` / `meta: hidden` поштучно); `HomePage.vue`: зняти `if (admin.value)` і `v-else-if="!admin"` — або лишити `StartSteps` студентам як блок «З чого почати» під текстом.
- `routes/guide.py`: `current_user` у двох GET + 404 для `DRAFT` не-адміну (як до T136); у смоуку — повернути «guest: draft 404».

## Перед показом сайту студентам

- Імпорт списків груп (`/students` → ЦІСТ-текст) **до** показу: без нього студент після входу — `pending`, без профілю й без лінка на бота (так задумано в T64/T58).
- Деплой — [T116](../T116-Q--guide-questions.md) Q14 (vps-infra); redirect URI прод-домену в GCP.

## Перевірка

- `DB_NAME=python_labs_smoke uv run python tests/smoke_guide.py` — 21/21: гість 401 на список і сторінку, студент 403 на список / сторінку / чернетку / історію, PUT 401/403, решта як було.
- `npm run build` — чисто. Ruff (0.16.3 з сусіднього venv, без конфігу проєкту) — лише давні стильові B008/RUF100, ті ж і в незмінених файлах.
- Headless Chrome (`--dump-dom`) гостем на 5030: `/` — h1 «Python Labs», callout, 3 × ⬜, «Увійти з Google»; `/labs`, `/method`, `/games` — «Потрібен вхід» + шлях у `<code>`.
- Не перевірено кліком: домашня очима студента (✅ на кроках) і pending (рядок ⏳) — логіка проста, чекає клік-тесту.
