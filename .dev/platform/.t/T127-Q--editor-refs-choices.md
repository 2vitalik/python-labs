# T127 · Редактор методички, приклади полів емодзі, ідеї в знахідках — вибори

`2026-09-21` · ❓ question · контекст — [T126](T126-P--guide-editor-v1.md) (редактор), [T125](T125--ui-polish-4/plan.md) п. 41, 45, 49

**Q1 — Підхід до редактора (п. 41).** Рекомендую **варіант 3** з T115 §4: тексти в Mongo, інлайн-редактор на сайті, історія через `record()`, файли `data/guide/` — seed/export (обґрунтування — T126 §1).
- a) **варіант 3** *(рекомендую)*;
- b) варіант 2 — веб-редактор GitHub + автопул на сервері: нуль коду, але лише після деплою, історія тільки в git, «окремий підрозділ» — ні;
- c) інше.

> 

**Q2 — Гранулярність (п. 32).**
- a) **✏️ на `#`, кожному `##` і `###`**, редактор інлайн під заголовком *(рекомендую)*;
- b) лише вся сторінка (простіше на ~40 рядків, але довгий textarea);
- c) окрема сторінка `/labs/1/edit` замість інлайну.

> 

**Q3 — «Що змінилось» (п. 35).** T126 §5.
- a) вручну, як зараз;
- b) **нотатка при збереженні → чернетка на `/changes`, публікуєш ти** *(рекомендую)*;
- c) b + кнопка «Скласти запис» моделлю (пізніше, після b).

> 

**Q4 — Приклади полів (п. 45).** T126 §6.
- a) **` ```field `-блоки → `Field.vue` клітинками** (однаково на всіх ОС) *(рекомендую)*;
- b) просто текст у `<pre>` — швидко, але сітка зʼїжджає між ОС;
- c) Markdown-таблиця з емодзі.
І де: в `game.md` під `<details>` «Приклади ситуацій» чи по лабах?

> 

**Q5 — Ідеї в знахідках (п. 49).** Пропоную **не ускладнювати**: `Ref.kind: link | idea` — ідея = запис без URL (заголовок + нотатка), необовʼязково «до якої знахідки» (`parent`, показується під нею з відступом); одна стрічка, перемикач «усе / лінки / ідеї»; категорій/тегів не робити, поки не назбирається 50+ і не буде видно, як групувати. Форма: друга кнопка «Записати ідею» поруч із «Кинути лінк».
- a) **так** *(рекомендую)*;
- b) окрема сторінка «Ідеї»;
- c) не треба — ідея = нотатка до лінка;
- d) інше (категорії одразу?).

> 

**Q6 — Кого трекати в `activity` (п. 31).** Зараз — усіх, хто увійшов, включно з тобою (фільтр «лише студенти» — при показі).
- a) **усіх** *(зараз)*;
- b) лише студентів.

> 
