# T122 · Уся методичка, студи, каталог, бот: 20 правок після клік-тесту T121 — план

`2026-09-21` · ⚙️ задача · запит Vitalik у чаті після клік-тесту [T121](../T121--nav-crumbs-polish/report.md) · звіт — [report.md](report.md)

Запит (чат, 2026-09-21), по пунктах:

1. `/guide`, зміст: «↗» відокремити і взяти в рамку — зараз не видно, що це окрема кнопка.
2. `/guide`, зміст: менший шрифт, менший інтервал між рядками.
3. Іконка «Що змінилось» (📰) — знову не та.
4. «Привіт, Віталій» → з «!».
5. Крихти — трохи вище, відступ знизу більший.
6. «Уся методичка» — інший URL замість `/guide`.
7. `/guide`: заголовки розділів клікабельні, але неоднозначно — якір і «відкрити окремо» іконками-кнопками праворуч.
8. Лаби: «Усі лаби» внизу не по центру (на 1 і 5 — зʼїжджає).
9. «Уся методичка» під кожною сторінкою розділу, справа внизу як на домашній.
10. `/games`, `/tasks` (адмін): текст розділу видимий за замовчуванням, у рамці, явно згортається, вибір памʼятається.
11. Поміняти місцями «Лаби» і «Ігри» в меню.
12. «＋» у шапці підзони тасків — з підписом («Новий таск» чи коротше).
13. Клік по зоні/підзоні — не нагору, а так, щоб «Завдання» було зверху.
14. Клік по аватару і імені в меню — у профіль.
15. `/students` — за замовчуванням «Таблиця».
16. Студи-таблиця: ПІБ + пошта дрібно під ним; група — не стовпець, а візуальне групування зі згортанням, «згорнути/розгорнути всі», окрема група — на підсторінці з крихтою.
17. Telegram у таблиці — клікабельний.
18. Callout у методичці — не синій, а жовтенький.
19. Спалах цілі `#якір` спрацьовує не завжди (на домашній — ні).
20. Запобіжник на «відвʼязати» бота — перепитувати.

План: (а) `/all` замість `/guide` + `GuideFoot.vue` (Оновлено · 📖 Уся методичка) під сторінками; `.ibtn` — спільна рамочна іконка-кнопка (зміст, заголовки розділів, ↗ групи); (б) `GuidePage` — grid `1fr auto 1fr` для ←·Усі лаби·→; (в) `GuideHead` — `<details>` у рамці з «згорнути ▴/розгорнути ▾», стан у `localStorage`; (г) `scrollBehavior` — зміна лише `?query` не скролить, зона/підзона → до `#catalog`; (д) `guide.js` — порядок Лаби · Ігри; (е) `NavUser` — аватар+імʼя лінком; (є) `StudentsTable.vue` — групи-`<tbody>` зі згортанням, `?group=` + крихта, t.me-лінки; (ж) `GuideText` сам спалахує ціль після рендера + глобальний watch хешу в `App.vue`; (з) callout на `warning`; (и) відвʼязка в два кліки.
