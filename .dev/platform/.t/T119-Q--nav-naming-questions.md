# T119 · Питання по меню й ширині: назва «Здача», ширина 52rem, зміст

`2026-09-20` · ❓ question · контекст — [T118](T118--nav-width-polish/report.md); мовчання = варіант, що вже в коді (позначено «зараз»).

**Q1 — Пункт меню для «Як здавати».** Одне коротке слово для сторінки про GitHub, заявки, відео, захисти, заняття, профіль і словник. Брейншторм:
- a) **Здача** *(зараз)* — буквально «здача лаб», 5 літер, звичне студентське слово; мінус — трохи розмовне;
- b) **Правила** — ширше за зміст сторінки, а «правила балів» уже в «Бали»;
- c) **Процес** / **Порядок** — точно по суті («як усе відбувається»), але абстрактно;
- d) **Формат** — «формат курсу», нейтрально, але не каже «як здавати»;
- e) **Як здати** — найясніше, але два слова (в меню не переноситься, бо nowrap, просто ширше);
- f) **Гайд** / **FAQ** — «гайд» уже зайнято `/guide` (уся методичка), FAQ — не той жанр;
- g) інше.
Slug `/howto` лишається будь-де — змінюється лише напис у `guide.js`.

> 

**Q2 — Ширина сторінки.** Одна змінна `--page-max: 52rem` (832 px) у `style.css` — усі сторінки й меню. Це між «профілем» на xl-екрані (760) і на xxl (880); `GuideHead` уже мав 52rem.
- a) ок *(зараз)*;
- b) вужче (48rem = 768) — коротші рядки для читання;
- c) ширше (56rem = 896);
- d) інше число.

> 

**Q3 — Широкі робочі сторінки.** `/tasks` і `/games/<slug>` (каталоги адміна) лишились на всю ширину вікна, меню при цьому — на ширині сторінки (по центру).
- a) ок, це робочі таблиці *(зараз)*;
- b) і їх у 52rem — тоді картки в 2 колонки;
- c) меню теж розтягувати на широких сторінках.

> 

**Q4 — Зміст guide-сторінки.** Тепер він фіксований у правому полі поза колонкою тексту, по центру вертикалі, і зʼявляється лише коли поле вміщає його: вікно ≥ 76rem (1216 px). Вужче — змісту нема (як раніше нижче lg).
- a) ок *(зараз)*;
- b) на вужчих вікнах повертати стовпчик усередину сторінки (текст ~620 px + зміст);
- c) поріг інший (наприклад, 70rem зі змістом 8rem).

> 

**Q5 — «Таски» в текстах.** У меню — «Таски», заголовок сторінки — «🧩 Таски», у прозі — «таск» там, де коротко, і «завдання з тем лаб» для лабораторних; словник пояснює.
- a) ок *(зараз)*;
- b) у текстах лише «завдання», «Таски» — тільки пункт меню.

> 

**Q6 — «Знахідки».** Прибрано з меню; кнопка «💡 Знахідки» поруч із «➕ Нова гра» / «➕ Нове завдання» на адмінських `/games` і `/tasks`, плюс лінк у тексті `tasks.md#ideas`.
- a) ок *(зараз)*;
- b) повернути в «Ще» для адміна.

> 
