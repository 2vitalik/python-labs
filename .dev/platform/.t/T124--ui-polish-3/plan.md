# T124 · Методичка /method, зміст справа, широкі Таски, студи: 27 правок — план

`2026-09-21` · ⚙️ задача · запит Vitalik у чаті після клік-тесту [T122](../T122--guide-students-polish/report.md) · звіт — [report.md](report.md)

Запит (чат, 2026-09-21), по пунктах:

1. Уся методичка — за адресою `/method` (замість `/all`).
2. Крихти на сторінках-розділах методички: між 🏠 і сторінкою — «Методичка».
3. `/method`, зміст: інтервал ще трішки менший.
4. «↗» — зліва від посилання у змісті і зліва від заголовка розділу.
5. Підказка «↗» — «Відкрити розділ окремою сторінкою».
6. Жовті callout-и: відступи зверху/знизу не рівні (зверху мініатюрний, знизу завеликий) — відцентрувати.
7. До заголовків розділів (справа, де «🔗») — кнопка «наверх, до змісту».
8. Домашня: «Що змінилось» стало синім із підкресленням — знову сірим, як «Уся методичка».
9. «Уся методичка» внизу сторінок — лишити (і зверху в крихтах, і знизу).
10. `/games` і `/tasks`: «згорнути» — синім, «розгорнути» — сірим.
11. Трикутнички згорнути/розгорнути всюди мініатюрні — замінити на щось помітніше.
12. Студи: менший інтервал ПІБ ↔ пошта, менша висота рядка, пошта світліша; в ідеалі група вміщається на екран.
13. Студи: нумерація рядків усередині групи, світло-сірим зліва, можна за межами таблиці.
14. Домашня, «Як влаштований курс»: Лаби перед Грою — як у меню.
15. Кнопка «Моя гра» справа в крихтах на `/games` (і `/tasks`), синя як на домашній — на телефоні вона ховається в «Ще».
16. `/tasks` — стандартна ширина; справа «Зміст» по категоріях, що зараз показані, як на інших сторінках.
17. Кнопка розгорнути `/tasks` на всю ширину і назад (іконка «стрілки в обидва боки»), вибір запамʼятовується.
18. Розгорнутий блок тексту на `/games` і `/tasks` — теж у «Зміст» справа (Vitalik не певен; поки — так).
19. Меню: замість «Python Labs» завжди іконка дому — щоб імʼя влазило.
20. Широкий `/tasks`: крихти і блок тексту — на стандартній ширині, широкий — лише каталог нижче.
21. Іконка Lode Runner: 🪙 → 🪜.
22. Студи: клік по рядку не відкриває профіль; клік по імені — відкриває.
23. Студи: колонку «Статус» прибрати; `admin`/`pending` — інший фон рядка.
24. Студи, Telegram: ✅ у всіх дратує — помічати навпаки: «без нікнейма» сірим курсивом, 🚫 якщо не привʼязав бота, хоч уже логінився.
25. Студи: кольором помітити тих, хто ще жодного разу не логінився.
26. Студи: «—» сіреньким і по центру колонки.
27. Студи: без hover-підсвітки рядка (кліку по рядку більше нема).

## План

- API: у списку студентів для адміна — `seen` (`seen_at is not None`) для п. 24–25.
- Роутер/лінки: `/all` → `/method` (п. 1), `GuideFoot`, README методички, `.dev`.
- `Crumbs`: слот праворуч (п. 15); «Методичка» у крихтах `GuidePage`, `/games`, `/tasks` (п. 2).
- `/method`: щільніший зміст, «↗» зліва, підказка, «↑» до змісту (п. 3–5, 7).
- `GuideText`: callout — рівні відступи (п. 6); `GuideFoot`: `:slotted(a)` (п. 8).
- `GuideHead`: шеврони-SVG замість ▴▾, «згорнути» синім, віддає зміст тексту нагору (п. 10, 11, 18); шеврон і в студах.
- `Toc.vue` — спільний фіксований «Зміст» справа: `GuidePage`, `/games`, `/tasks` (текст + зони) (п. 16, 18).
- `/tasks`: стандартна ширина + перемикач «на всю ширину» з памʼяттю; верх сторінки завжди стандартний (п. 16, 17, 20); `wide.js` спільний для `App.vue`.
- `NavBar`: бренд — завжди іконка (п. 19).
- `StudentsTable`: щільні рядки, нумерація CSS-лічильником поза таблицею, імʼя-лінк, без «Статус» і hover, фон рядка за статусом і «не заходив», Telegram-стани, «—» по центру (п. 12, 13, 22–27).
- Дані: `home.md` — Лаби перед Грою (п. 14); `games.yaml` + Mongo — 🪜 (п. 21).
