# T128 · Редактор методички v1: правки на сайті, історія, чернетка «Що змінилось», поля-клітинки, ідеї в знахідках — план

`2026-09-22` · ⚙️ задача · запит Vitalik у чаті · пропозиція — [T126](../T126-P--guide-editor-v1.md) · вибори — [T127](../T127-Q--editor-refs-choices.md) · звіт — [report.md](report.md)

Запит (чат, 2026-09-22): «Давай зробимо редактор, і всі інші штуки, все за твоїми рекомендаціями. І давай може поміняємо лінк /profile на /my/profile? Як думаєш, так буде краще чи ні? Типу за аналогією з /my/game».

## Рішення (усі рекомендації T127 — «а», прийнято в чаті)

- Q1 — варіант 3 з T115 §4: тексти в Mongo, інлайн-редактор на сайті, файли = seed/export.
- Q2 — ✏️ на кожному `##`/`###` + «✏️ сторінку» для всієї сторінки, редактор інлайн під заголовком.
- Q3 — нотатка при збереженні → чернетка на `/changes`, публікує Vitalik (5b); 5c (модель) — потім.
- Q4 — ` ```field `-блоки → сітка клітинок; приклади — у `game.md` під `<details>` (усі пʼять ігор з Notion).
- Q5 — ідеї в знахідках: `Ref.kind: link | idea`, `parent`, одна стрічка з фільтром.
- Q6 — трекати всіх (як є, без змін у коді).
- `/profile` → `/my/profile` — так: усе особисте під `/my/` (гра, профіль, далі заявки/бали), старий шлях лишається редиректом (лінки з уже надісланих повідомлень бота).

## Кроки (T126 §7, обидві сесії за раз)

1. Бек: модель `guide` (`slug, title, body, rev, updated_at, updated_by`), `guide_io.py` (seed при старті / export / import), `PUT /api/guide/{slug}` з `rev` → 409, `note` у `history`, `GET /api/guide/history?slug=`, чернетка `changes-draft` + `POST /api/guide/changes/publish`; смоук.
2. Фронт: `GuideBody.vue` (текст + інструменти адміна) на всіх сторінках з текстом методички, `GuideEditor.vue` (Teleport у слот після заголовка, textarea + превʼю, нотатка, гарячі клавіші, guard), ✏️ у `md.js`, вирізання/вклеювання розділу (`headings.js`).
3. `/method/history`: список правок з `+N −M`, рядковий diff (`diff.js`, LCS + згортання контексту), «↩» відкат новою правкою.
4. `/changes`: блок чернетки (`ChangesDraft.vue`) — «↑» у публічне / «✕».
5. ` ```field ` → `field.js` + CSS; перенос прикладів з `data/v2/notion/games/` у `game.md`.
6. Знахідки: `kind`/`parent` у моделі й API, `RefCard.vue`, фільтр, ідея під знахідкою.
7. `/my/profile`: маршрут + редирект, меню, аватар, домашня, бот, тексти методички, тест.
8. Доки: `data/guide/README.md` (правда — Mongo), `app/README.md`, README вузла/кореня, CHANGELOG, STATUS.
